<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.permission.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.compass"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.light"
        android:required="true" />
    <uses-feature
        android:name="android.permission.sensor.proximity"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tombol_biru"
        android:label="@string/app_name"
        android:roundIcon="@drawable/applogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LsensorActivity"></activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".RotationActivity" />
        <activity android:name=".RelativeActivity" />
        <activity android:name=".ProximityActivity" />
        <activity android:name=".MagneticActivity" />
        <activity android:name=".LinearActivity" />
        <activity android:name=".LightActivity" />
        <activity android:name=".GyroscopeActivity" />
        <activity android:name=".GravityActivity" />
        <activity android:name=".AccelerometerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>